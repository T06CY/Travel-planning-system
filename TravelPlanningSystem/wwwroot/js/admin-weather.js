(() => { const c = { "kuala lumpur": [3.139, 101.687], kuching: [1.553, 110.359], penang: [5.414, 100.329], "kota kinabalu": [5.98, 116.073], "johor bahru": [1.492, 103.741], "hong kong": [22.319, 114.169], singapore: [1.352, 103.82], bangkok: [13.756, 100.502], jakarta: [-6.208, 106.845], bali: [-8.65, 115.216], seoul: [37.566, 126.978], tokyo: [35.676, 139.65], shanghai: [31.23, 121.474] }; const t = x => x <= 1 ? "Clear" : x <= 3 ? "Cloudy" : x <= 67 ? "Rain" : x <= 77 ? "Snow" : "Storm"; const risk = x => x.wind_speed_10m >= 40 || x.visibility < 5000 || x.rain >= 5 || x.weather_code >= 95 ? ["High risk", "high"] : x.wind_speed_10m >= 25 || x.rain > 0 || x.weather_code >= 51 ? ["Monitor", "medium"] : ["Normal", "low"]; document.querySelectorAll("[data-weather-city]").forEach(async e => { const p = c[(e.dataset.weatherCity || "").trim().toLowerCase()]; if (!p) { e.innerHTML = '<span class="weather-unavailable">Unavailable</span>'; return } try { const r = await fetch(`https://api.open-meteo.com/v1/forecast?latitude=${p[0]}&longitude=${p[1]}&current=temperature_2m,rain,wind_speed_10m,visibility,weather_code&timezone=auto`); const x = (await r.json()).current, [a, b] = risk(x); e.innerHTML = `<span class="weather-risk weather-${b}"><i class="bi bi-cloud-sun"></i>${a}</span><small>${t(x.weather_code)} · ${Math.round(x.temperature_2m)}°C · ${Number(x.rain).toFixed(1)} mm rain</small><small>${Math.round(x.wind_speed_10m)} km/h wind · ${Math.round(x.visibility / 1000)} km visibility</small>` } catch { e.innerHTML = '<span class="weather-unavailable">Temporarily unavailable</span>' } }) })();
